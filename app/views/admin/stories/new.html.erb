<h1>New Story</h1>

<%= render 'form', story: @story, requesters: @requesters,
  story_states: @story_states, project: @project, story_types: @story_types, groups: @groups %>

<%= link_to 'Back', @project %>
