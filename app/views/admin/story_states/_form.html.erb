<%= form_with(model: story_state, local: true) do |form| %>
  <% if story_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(story_state.errors.count, "error") %> prohibited this story_state from being saved:</h2>

      <ul>
      <% story_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :story_state_name %>
  </div>

  <div class="field">
    <%= form.label :display_name %>
    <%= form.text_field :display_name, id: :story_state_display_name %>
  </div>

  <div class="field">
    <%= form.label :icon %>
    <%= form.text_field :icon, id: :story_state_icon %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
