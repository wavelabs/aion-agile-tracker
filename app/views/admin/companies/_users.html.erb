<fieldset>
  <legend>Users (<%= companies_users.count %>)</legend>
  <table>
    <thead>
      <tr>
        <th>Email</th>
        <th>Role</th>
      </tr>
    </thead>
    <tbody>
      <%= render collection: companies_users, as: :company_user, partial: 'admin/companies_users/companies_user' %>
    </tbody>
  </table>
</fieldset>
