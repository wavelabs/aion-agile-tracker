<%= form_with(model: task, local: true) do |form| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.select :project_id, projects.pluck(:name, :id), id: :task_project_id %>
  </div>

  <div class="field">
    <%= form.label :worked %>
    <%= form.text_field :worked, id: :task_worked %>
  </div>

  <div class="field">
    <%= form.label :billed %>
    <%= form.text_field :billed, id: :task_billed %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :task_description %>
  </div>

  <div class="field">
    <%= form.label :date_started %>
    <%= form.date_select :date_started, id: :task_date_started %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
